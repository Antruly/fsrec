#pragma once
// Central product identity. Used by the startup banner, the /ping handler,
// and mirrored in the Windows version resource (resources/fsrec.rc) and the
// Inno Setup script (packaging/fsrec.iss). Keep all three in sync.

#define FSREC_NAME "fsrec"
#define FSREC_VERSION "1.0.8"
